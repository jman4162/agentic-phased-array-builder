---
description: API reference for the Strands Agents adapter and the deterministic LangGraph pipeline.
---

# Adapters

External-framework frontends for APAB's MCP tools. Each has its own
optional extra (`apab[strands]`, `apab[langgraph]`).

::: apab.adapters.strands

::: apab.adapters.langgraph_pipeline
